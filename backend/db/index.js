/**
 * this index file will be used to export all
 * of the ORM objects in this DB folder
 */

 const recipe = require('./recipe')
 const users = require('./users')
 

 module.exports = {recipe, users}